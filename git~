1. gremo na https://github.com  in se logiramo na signin z  user:pavlovicr  password:rolu9255
2. z izbiro +new repository vnesemo ime za novo shrambo napr. vaja
3.create repository
in to je to.

sedaj še la lokalnem računalniku
1.v terminalu izberem nek direktorij /hec
2.na /hec zaženem git init, ki ustvari poddirektorij .git
3.zaženem še git clone https://github.com/pavlovicr/vaja
in evo v dirktoriju hec ustvari direktorij vaja

in sedaj še tretji korak. 
1.v direktoriju vaja ustvarimo fajl zabava
2.zaženemo: git add zabava
          : git commit -m 'posodobitev ali karkoli'
          : git push -u origin master
in imamo naš fajl na portalu github.com


še o geslih :

1. glavne skupne informacije so na /home/pavlovicr/.gitconfig, ki ga ustvari po instalaciji GitHub programa
2. informacije konkretnega repositoriya pa v fajlu npr. VAJA/.git/config


po instalaciji (??) apt-get install git dobimo fajl :
/home/pavlovicr/.gitconfig v katerem so shranjene glavne skupne informacije.

ime in geslo vnesemo lahko s komando :

$ git config --global user.name "pavlovicr"
$ git config --global user.email pavlovicrados@gmail.com

oziroma brez --global , ko gr samo za dir VAJA




Git has a number of different transfer protocols you can use. The previous example uses the https:// protocol, but you may also see git:// or user@server:path/to/repo.git, which uses the SSH transfer protocol. “Getting Git on a Server” will introduce all of the available options the server can set up to access your Git repository and the pros and cons of each.
























2. v failu VAJA/.git/config pa samo za ta direktorij


komanda :
$ git config --global user.name "pavlovicr"
$ git config --global user.email pavlovicrados@gmail.com

oziroma brez --global , ko gr samo za dir VAJA




Git has a number of different transfer protocols you can use. The previous example uses the https:// protocol, but you may also see git:// or user@server:path/to/repo.git, which uses the SSH transfer protocol. “Getting Git on a Server” will introduce all of the available options the server can set up to access your Git repository and the pros and cons of each.
